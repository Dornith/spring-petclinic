database=postgres
spring.datasource.url=jdbc:postgresql://my-psql.postgres.database.azure.com:5432/petclinic?sslmode=require
spring.datasource.username=${DB_USERNAME:xspila}
spring.datasource.password=${DB_PASSWORD:Placeholder}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=never

